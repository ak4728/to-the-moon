name: Python package

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Main tests
        run:  sudo echo "/usr/local/bin/" >> /etc/paths
